SUBDIRS= src

JARFILE = qlist.jar
jardir=$(pkgdatadir)/packages/qlist/java
jar_DATA = $(JARFILE)

clean-compile:
	rm -f $(JARFILE)

$(JARFILE):
	( cd src; jar -cvf ../$(JARFILE) `find ircam -name "*.class"` )
.PHONY: $(JARFILE)
