#SUBDIRS=control data explode guiobj io ispw ispwmath lists math midi numeric qlist sequence signal system utils @PLATFORM_PACKAGES@
# Attention !!! The order is important: since some packages are linked versus others,
# the linked packages must be compiled first
# Note: the order can be derived from the "require" order
SUBDIRS=utils control data guiobj io ispw ispwmath lists math midi numeric qlist signal system @PLATFORM_PACKAGES@

